{
  "name": "shoof",
  "version": "1.0.0",
  "description": "A social app focusing on authentication and best practices. Made with tRPC.",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm -r --parallel --filter='./packages/*' run dev",
    "test": "echo \"Error: no test specified\" && exit 0",
    "prepare": "husky install",
    "cm": "cz"
  },
  "author": "leoncik",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^18.4.3",
    "@commitlint/config-conventional": "^18.4.3",
    "commitizen": "^4.3.0",
    "husky": "^8.0.0",
    "validate-branch-name": "^1.3.0"
  }
}
